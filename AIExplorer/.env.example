# ============================================================================
# API Configuration
# ============================================================================

# API Base URL (auto-detected if not set)
# Leave empty for auto-detection: localhost:8000 for dev, production URL for prod
# NEXT_PUBLIC_API_URL=http://localhost:8000

# ============================================================================
# Azure MSAL (Authentication)
# ============================================================================

# Azure AD Application (Client) ID
NEXT_PUBLIC_MSAL_CLIENT_ID=your_msal_client_id_here

# Azure AD Authority URL
# Format: https://login.microsoftonline.com/YOUR_TENANT_ID
NEXT_PUBLIC_MSAL_AUTHORITY=https://login.microsoftonline.com/your_tenant_id

# Application redirect URI (must match Azure AD app registration)
NEXT_PUBLIC_MSAL_REDIRECT_URI=http://localhost:3000

# ============================================================================
# Database Configuration (Azure SQL)
# ============================================================================

# SQL Server hostname/IP
DB_SERVER=your_server_address.database.windows.net

# Database name
DB_DATABASE=your_database_name

# Database username (should use service principal or managed identity)
DB_USERNAME=your_database_user@your_server

# Database password (keep secure, use secrets management in production)
DB_PASSWORD=your_database_password

# ============================================================================
# Power Automate
# ============================================================================

# Power Automate Approval Flow Webhook URL
# This is used for triggering approval workflows when use cases are created
POWER_AUTOMATE_APPROVAL_FLOW_URL=https://prod-xx.northeurope.logic.azure.com:443/workflows/xxx/triggers/manual/paths/invoke

# ============================================================================
# Application Environment
# ============================================================================

# Node environment (development, production, test)
NODE_ENV=development

# ============================================================================
# Feature Flags
# ============================================================================

# Enable debug logging in production (be cautious with this)
# DEBUG=true

# ============================================================================
# Notes
# ============================================================================

# 1. Never commit .env.local to version control
# 2. Use .env.example for documenting required variables
# 3. For production:
#    - Use environment variables from your hosting platform (Vercel, Docker, etc.)
#    - Store sensitive values in secrets management systems
#    - Never hardcode credentials
# 4. Test all environment variables after deployment
# 5. Keep database passwords secure and rotate regularly
